import { queryAPI } from "../src/client/js/queryAPI";

describe('testing queryAPI module functionality', () => {
    test('testing that the function present', () => {
        expect(queryAPI).toBeDefined()
    })
})