import { fadeIn } from '../src/client/js/fadeIn'

describe('testing the fadeIn fucntionality', () => {
    test('testing fadeIn module is present & fucntioning', () => {
        expect(fadeIn).toBeDefined()
    })
})