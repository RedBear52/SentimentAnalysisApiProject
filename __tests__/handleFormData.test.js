import { handleFormData } from "../src/client/js/handleFormData";

describe('testing handleFormData module functionality', () => {
    test('testing handleFormData functionality', () => {
        expect(handleFormData).toBeDefined()
    })
})